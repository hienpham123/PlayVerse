[build]
  base = "client"
  publish = "build"
  command = "npm install && npm run build"
  working_dir = "client"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

